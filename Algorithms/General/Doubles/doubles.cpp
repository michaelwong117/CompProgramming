// An easy way to output the answer is to use the printf 
// function and give the number of decimal places in the formatting string.


// For example, the following code prints the value of x with 9 decimal places:
printf("%.9f\n", x);

if(abs(a-b) < 1e-9) {
    // a and b are equal
}


cout << setprecision(5) << f << '\n';
