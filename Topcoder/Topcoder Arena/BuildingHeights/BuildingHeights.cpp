#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <set>
#include <algorithm>
#include <queue>
#include <stack>
#include <map>

#define ll long long

using namespace std;

class BuildingHeights {
    int minimum(vector<int> v) {
    	
    }
};


int main(){
	// ifstream fin("input.in");
	// string s, f;
	// fin >> s >> f;
	// vector<string> con;
	// for(int i = 0; i < 4; i++){
	// 	string constraint;
	// 	fin >> constraint;
	// 	con.push_back(constraint);

	// }
	// bfs(s, f, con);
	// cout << (char) 122 << endl;
	// cout << (int) 'a' << endl;


}